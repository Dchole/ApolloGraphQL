query GetLaunchDetails($id: ID!) {
  launch(id: $id) {
    id
    site
    details
    mission {
      name
      missionPatch
    }
    rocket {
      id
      name
      type
    }
    links {
      article
      video
    }
    isBooked
  }
}
