import { createMuiTheme } from "@material-ui/core";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#15CBC4"
    },
    secondary: {
      main: "#F7E5CE"
    },
    background: {
      default: "#FBF2E7"
    }
  }
});

export default theme;
